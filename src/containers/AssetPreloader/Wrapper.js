// Import dependencies
import styled from 'styled-components';

// Export the styled component
export default styled.div`
  height: 0;
  opacity: 0;
  overflow: hidden;
  pointer-events: none;
  width: 0;
`;
